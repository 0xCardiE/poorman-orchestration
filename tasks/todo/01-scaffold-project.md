Goal:
- Create the initial app scaffold for the Niche Research Digest project.

Acceptance criteria:
- Set up a Vite + React + TypeScript project in this repository.
- Add ESLint and Vitest if they are not already present.
- Create a sensible folder structure that matches `AGENTS.md`.
- Add or update `README.md` with setup and available commands.
- Confirm the app builds successfully.

Constraints:
- Keep the scaffold minimal and clean.
- Avoid unnecessary dependencies.
- Do not implement product features yet beyond a minimal shell if needed.
